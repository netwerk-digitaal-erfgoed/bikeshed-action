name: 'Publish Bikeshed document'
description: 'Publish your Bikeshed source to HTML'
runs:
  using: 'docker'
  image: 'docker://netwerkdigitaalerfgoed/bikeshed:1.4.4'
  args:
    - ${{ inputs.source }}
    - ${{ inputs.destination }}
